apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-doom-svc
  labels:
    app: {{ .Release.Name }}-doom
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Release.Name }}-doom
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
